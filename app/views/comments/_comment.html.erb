<%= div_for(comment) do %>
	<%= h(comment.body) %>
	<p class:"comment">
		<strong>Posted <%= time_ago_in_words(comment.created_at) %> ago by <%= h(comment.author) %></strong></br>
	</p>
	<%= link_to 'Destroy', comment, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>	
