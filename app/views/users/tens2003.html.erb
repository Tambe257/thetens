<div class="profilepic">
	<h1><%= link_to (image_tag @user.image(:small)), @user %>     <%= @user.name %> - Top Ten of 2003</h1>
</div>
<div id="pins"> 
	<%= render @pins.where(:date => '2003').order('rank DESC') %>
</div>
<%= will_paginate @pins %>