<div class="user_show">
	<div class="profilepic">
		<h1><%= link_to (image_tag @user.image(:small)), @user %>     <%= @user.name %> - #1 Albums </h1>
	</div>
	<div id="pins">
	  <%= render @pins.where(:rank => 1) %>
	</div>
	<%= will_paginate @pins %>
</div>	