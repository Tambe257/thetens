<% if user_signed_in? %>
<h4>Here are the new reviews!</h4>
<div id="pins">
  <%= render @pins[0, 10] %>
</div> 

<h4>Scroll down to browse the review library...</h4>
<% else %>

<div class="center hero-unit">
	<h1>Welcome to theTens!</h1>
	<p>
	  Where Your Opinion Matters
	</p>
	<p>
	  <%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
	</p>
</div>

<h4>Here are the new reviews!</h4>
<div id="pins">
  <%= render @pins[0, 10] %>
</div> 

<h4>Scroll down to browse the review library...</h4>
<% end %>