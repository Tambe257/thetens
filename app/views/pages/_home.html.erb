<% if user_signed_in? %>
	<h4>Click The Album Below To See The Review</h4>

<% else %>

<div class="center hero-unit">
	<h1>Welcome to theTens!</h1>
	<p>
	  Where Your Opinion Matters
	</p>
	<p>
	  <%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
	</p>
</div>

<% end %>