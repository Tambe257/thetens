<h1>Check Out Who's Reviewing Albums on theTens...</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to (image_tag user.image(:small)), user %> <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>